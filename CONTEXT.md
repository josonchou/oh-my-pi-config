# OMP 通知集成

定义 OMP、PeonPing 与 cmux 在本机开发工作流中的通知边界，避免不同通知渠道重复表达同一个事件。

## Language

**共享 Peon 运行时**：位于 `~/.openpeon`、由 OMP 与 OpenCode 共用的 PeonPing 数据根，包含配置、语音包与状态。
_Avoid_: Claude 配置、OMP 私有运行时

**通知主通道**：负责向用户主动发出需关注事件的唯一渠道。当前决策为 PeonPing。
_Avoid_: 双重弹窗、所有渠道都通知

**状态面板**：记录未读状态并支持跳转，但不作为常规声音或桌面打扰的渠道。当前决策为 cmux。
_Avoid_: 主通知通道

**长任务完成**：仅当有 UI 的主交互会话连续执行至少 30 秒后结束的成功完成信号。
_Avoid_: 每轮完成、子代理完成

**会话问候**：有 UI 的主交互会话启动时发出的 PeonPing 提示音；即使当前终端聚焦，也必须可听见。
_Avoid_: 无 UI 的 print 模式与子代理启动、前台静音

**单次工具失败**：一次工具调用的失败结果，只通过所属代理的终端状态和输出呈现，不进入 PeonPing 主通知。
_Avoid_: 任务失败、需人工处理

**人工阻塞**：主交互会话正在等待用户授予工具审批的状态，必须立即通过 PeonPing 提醒。
_Avoid_: 普通错误、后台状态

**自动压缩**：OMP 的内部上下文维护事件，只保留在终端记录中，不进入 PeonPing 主通知。
_Avoid_: 资源告警、人工阻塞

**可提醒完成**：没有任何工具失败且满足长任务完成条件的主会话完成信号。
_Avoid_: 含失败的完成、未经验证的成功

**静默状态提示**：cmux 的 Dock 徽标、菜单栏入口与未读 pane 环；不包含声音、闪烁或 workspace 重排。
_Avoid_: 第二套桌面通知、主动打扰

**共享 Peon 偏好**：OMP 与 OpenCode 共用的语音包、音量、启用状态、桌面通知、轮换和目录规则；完成事件的过滤策略仍由各自适配器拥有。
_Avoid_: 共享事件过滤、双份语音包
